feat(tts): switch to Gemini 2.5 Flash Preview TTS with Charon voice
- Replace Google Cloud TTS with Gemini 2.5 Flash Preview TTS in `server.js`
- Update `PersonalizedMessage.jsx` to handle Gemini response format
- Implement client-side WAV header generation for raw PCM audio
- Remove deprecated Cloud TTS error handling

feat(veo): upgrade to Veo 3.1 Fast Preview model
- Update model name to `veo-3.1-fast-generate-preview` in `server.js`

fix(server): resolve Express 5 breaking change
- Update catch-all route pattern from `*` to `/(.*)/` to fix server startup crash

ops(deploy): add deployment scripts and docs
- Add `deploy.sh` for Cloud Run deployment
- Add `SERVICE_ACCOUNT_SETUP.md` guide for IAM configuration

style(ui): update application icon
- Replace `vite.svg` favicon with custom "red forest" icon

docs: update README
- Reflect changes to TTS and Veo models
- Update troubleshooting and setup instructions